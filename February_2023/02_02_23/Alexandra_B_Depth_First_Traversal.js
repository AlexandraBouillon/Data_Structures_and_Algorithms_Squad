// *** WIP ***
// /*
// Depth First Traversal, given a node given a node,
//     visit all of it's descendants before visiting it's sibling

// To travel a tree in DFT we will use a stack (LIFO)
// adding to the top and removing from the top
// */

// const depth_first_traversal = () => {
//     let stack_of_nodes = [];

//     while(stack_of_nodes.length){
//         let new_node = stack_of_nodes.pop(); // removes last element
//         let next_node = null;
//         console.log('node_value', new_node);
//         if (new_node)
//         if ()

//     }
// };
