const is_flatten_binary_tree_to_linked_list = (binary_tree) => {
    // if (binary_tree.length === null) return null;
    // if (binary_tree.length === 0) return [0];
    // let node = 0;
    // let node_index = node;
    // // let last_node = binary_tree.length-1;
    // // let next_to_last_node = binary_tree.length-2;
    // let flattened = [];
    // while (binary_tree.length < 0){
    //     if (node !== null){
    //         flattened[node_index] === binary_tree[node]
    //     }
    // }



};

console.log(is_flatten_binary_tree_to_linked_list([1,2,5,3,4,null,6])); //[1,null,2,null,3,null,4,null,5,null,6]
console.log(is_flatten_binary_tree_to_linked_list([0])); // [0]
